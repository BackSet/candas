"""
URLs para el módulo Catalog
Todas las URLs HTML han sido eliminadas. Solo se usa la API REST.
"""
from django.urls import path

app_name = 'catalog'

urlpatterns = [
    # Sin URLs - Solo API REST
]
